# numpy
Calculator using NumPy is a simple Python program that performs basic arithmetic operations such as addition, subtraction, multiplication, and division using the NumPy library. It allows fast and accurate calculations and can handle multiple values at once, making it useful for learning numerical computation in Python.
